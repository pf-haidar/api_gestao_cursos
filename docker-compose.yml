version: '3.8'

services:
  postgres:
    container_name: desafio01_postgres
    image: postgres
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=desafio01_postgres